{
    "name": "finance-manager-devcontainer",
    "dockerComposeFile": "../docker/dev/docker-compose.yml",
    "service": "fastapi",
    "workspaceFolder": "/app",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-python.vscode-pylance",
                "ms-python.debugpy",
                "charliermarsh.ruff"
            ]
        }
    },
    "remoteEnv": {
        "PYTHONUNBUFFERED": "1",
        "PYTHONDONTWRITEBYTECODE": "1"
    },
    "runServices": [
        "postgres",
        "fastapi"
    ],
    "forwardPorts": [
        5432,
        8000
    ],
    "portsAttributes": {
        "5432": {
            "label": "FinanceManagerPostgres"
        },
        "8000": {
            "label": "FinanceManagerFastAPI"
        }
    },
    "mounts": [
        "source=${env:HOME}/.ssh,target=/home/appuser/.ssh,type=bind",
        "source=${env:HOME}/.gitconfig,target=/home/appuser/.gitconfig,type=bind"
    ]
}