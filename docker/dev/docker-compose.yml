services:
    api:
        container_name: finance-manager-api-dev
        init: true
        restart: unless-stopped
        build:
            context: ../../
            dockerfile: ./docker/dev/Dockerfile
        env_file:
            - .env
        ports:
            - "8000:8000"
